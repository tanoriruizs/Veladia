name: Release

# publica la extensión compilada como zip al crear un tag vX.Y.Z
on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Build
        run: npm run build

      - name: Empaquetar dist
        run: cd dist && zip -r "../veladia-${GITHUB_REF_NAME}.zip" .

      - name: Crear release en GitHub
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" "veladia-$GITHUB_REF_NAME.zip" \
            --title "Veladia $GITHUB_REF_NAME" \
            --generate-notes
